/*const bodyElement = document.body;

function adjustLayoutForscreen() {
  const screenWith = window.innerWidth;
  const bodyElement = document.body;
  // Add or remove classes based on live screen size
  if (screenWith < 312) {
    bodyElement.classList.add("is-small-mobile");
    bodyElement.classList.remove("is-tablet");
  } else {
    bodyElement.classList.remove("is-small-mobile");
    bodyElement.classList.add("is-tablet");
  }
}
window.addEventListener("DOMContentLoaded", adjustLayoutForscreen);
window.addEventListener("resize", adjustLayoutForscreen);*/
